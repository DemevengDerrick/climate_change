% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_utils.R
\name{exp_compute}
\alias{exp_compute}
\title{Compute exposed population from a flood fraction raster and a population raster}
\usage{
exp_compute(
  pop_ras,
  flood_frac_ras,
  pop_layer_name = "pop_total",
  exposed_layer_name = "pop_exposed"
)
}
\arguments{
\item{pop_ras}{SpatRaster. Population counts per cell.}

\item{flood_frac_ras}{SpatRaster. Flood fraction at population resolution
(output of \code{\link[=fld_aggregate_to_pop]{fld_aggregate_to_pop()}}).}

\item{pop_layer_name}{Character. Layer name assigned to the total
population output (default \code{"pop_total"}).}

\item{exposed_layer_name}{Character. Layer name assigned to the exposure
output (default \code{"pop_exposed"}).}
}
\value{
Named list with two SpatRasters:
\describe{
\item{pop_total}{Total population raster (renamed).}
\item{pop_exposed}{Exposed population raster.}
}
}
\description{
Elementwise multiplication of population count by flood fraction (values in
\link{0, 1}) to yield the number of people exposed within each grid cell.
}
