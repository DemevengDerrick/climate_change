% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_utils.R
\name{exp_run_country}
\alias{exp_run_country}
\title{Run the full country-level flood exposure pipeline for multiple population years}
\usage{
exp_run_country(
  pop_files,
  flood_frac_ras,
  ctry_boundary,
  admin_zones,
  indicator_name = "Women 15-49 exposed to RP100 floods",
  indicator_code = "wraf100",
  admin_level = 2L
)
}
\arguments{
\item{pop_files}{Character vector. Paths to population raster files
(one per year).}

\item{flood_frac_ras}{SpatRaster. Flood fraction raster at population
resolution (output of \code{\link[=fld_aggregate_to_pop]{fld_aggregate_to_pop()}}). Computed once and reused.}

\item{ctry_boundary}{sf or SpatVector. Country boundary (admin0) for
clipping population rasters.}

\item{admin_zones}{sf or SpatVector. Admin-level zones for zonal stats
(e.g. admin2).}

\item{indicator_name}{Character. Human-readable indicator name.}

\item{indicator_code}{Character. Short indicator code.}

\item{admin_level}{Integer. Administrative level of \code{admin_zones}
(default \code{2L}).}
}
\value{
Tibble of indicators with sequential \code{id} column filled.
}
\description{
Loops over population raster files, clips each to the country boundary,
computes exposed population using a pre-built flood fraction raster, runs
zonal statistics at the given admin level, and accumulates results into a
standardised indicator tibble.
}
