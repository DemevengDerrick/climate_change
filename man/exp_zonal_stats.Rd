% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exposure_utils.R
\name{exp_zonal_stats}
\alias{exp_zonal_stats}
\title{Compute zonal statistics for population exposure at an admin level}
\usage{
exp_zonal_stats(exposed_ras, pop_ras, zones)
}
\arguments{
\item{exposed_ras}{SpatRaster. Exposed population raster (named \code{"pop_exposed"}).}

\item{pop_ras}{SpatRaster. Total population raster (named \code{"pop_total"}).}

\item{zones}{sf or SpatVector. Zone polygons (e.g. admin2 boundaries).}
}
\value{
sf data frame with all zone attributes plus columns:
\code{pop_exposed}, \code{pop_total}, \code{perc_exposed}.
}
\description{
Runs \code{terra::zonal()} (sum) for both the exposed and total population
rasters against the same zone polygons, then joins the results and
computes the percentage exposed.
}
